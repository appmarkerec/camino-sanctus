name: camino_sanctus
description: "Bible Chat AI App with Voice Recognition"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0  # Add this line
  flutter_tts: ^3.8.5
  cupertino_icons: ^1.0.6
  hive_flutter: ^1.1.0
  path_provider: ^2.1.1
  speech_to_text: ^6.6.0
  google_generative_ai: ^0.2.2
  http: ^1.1.0
  get_it: ^7.7.0  # ← ADD THIS LINE

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.7
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
